import { moduleFor, test } from 'ember-qunit';

moduleFor('view:scan/not-found', 'Unit | View | scan/not found');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
