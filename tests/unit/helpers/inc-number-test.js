import {
  incNumber
} from '../../../helpers/inc-number';
import { module, test } from 'qunit';

module('IncNumberHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = incNumber(42);
  assert.ok(result);
});
